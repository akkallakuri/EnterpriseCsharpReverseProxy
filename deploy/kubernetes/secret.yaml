# DO NOT store real secrets here in plain text.
# Use `kubectl create secret` or a secrets manager (Vault, AWS Secrets Manager, Azure Key Vault).
#
# Example (run once, never commit):
#   kubectl create secret generic control-plane-secret \
#     --namespace reverse-proxy \
#     --from-literal=api-key='<your-strong-key>'
#
# The placeholder below is for reference only.
apiVersion: v1
kind: Secret
metadata:
  name: control-plane-secret
  namespace: reverse-proxy
type: Opaque
stringData:
  api-key: "REPLACE_BEFORE_APPLY"
